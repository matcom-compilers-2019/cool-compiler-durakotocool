class Main
{
   main():Int{
     {
        -- El predicado de while solo debe de ser de tipo bool
        let a:Int<-1 in while 1 loop a<-a+1 pool;
        -- El predicado del if solo debe de ser de tipo bool
        if new A then 1 else 0 fi;
     }
   };
};
class A
{
   sum(p1:Int,p2:Int):Int{ p1+p2};
};
